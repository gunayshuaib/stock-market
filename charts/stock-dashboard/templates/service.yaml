apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-dashboard.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "stock-dashboard.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
