apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stock-dashboard
spec:
  rules:
  - host: dashboard.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: stock-dashboard
            port:
              number: 5000
